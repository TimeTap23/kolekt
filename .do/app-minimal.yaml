name: threadstorm-minimal
services:
- name: threadstorm-app
  source_dir: /
  github:
    repo: TimeTap23/kolekt
    branch: main
  run_command: uvicorn start_kolekt:app --host 0.0.0.0 --port 8080
  environment_slug: python
  instance_count: 1
  instance_size_slug: basic-xs
  routes:
  - path: /
